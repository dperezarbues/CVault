# dependencies
/node_modules

# Next.js build output
/.next
/out

# production
/build

# misc
.DS_Store
*.pem
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# CV personal data — keep private, copy cv.example.json → cv.json to get started
src/data/cv.json
src/data/cv_v*.json

# QR code — generated at compile time, may encode personal data
public/qr-temp.svg

# Editor scratch layout — generated by the /templates layout editor, not for committing
src/layouts/editor.json

# Generated PDFs contain personal data
public/*.pdf

# WASM assets — generated by scripts/setup-wasm.sh, not committed
public/wasm/
public/typst/

# Fonts are committed (public/fonts/) — small static assets with no reliable CDN source
